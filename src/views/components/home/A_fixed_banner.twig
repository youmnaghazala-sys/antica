{% set c = component %}
<section>
	<div class="grid grid-cols-2 sm:grid-cols-4 grid-rows-2">
    {% for item in c.albome %}
        
        <div class="{{ loop.first ? 'col-span-2 row-span-2' : 'order-2' }}">
        {% if item.is_video %}
        	<video autoplay muted playsinline loop preload="none" loading="eager" class="object-cover" aria-label="Video banner">
				<source src="{{ item.video_url }}" type="video/mp4">
			</video>
        {% else %}
            <img src="{{item.banner_img}}" alt="image of banner" class="w-full h-full object-cover">
        {% endif %}
        
        </div>
    {% endfor %}
		<div class="text-center col-span-2 order-1 m-auto">
			<h1 class="text-[40px] font-bold" style="color:{{c.text_color}}">{{c.main_title}}</h1>
			<p class="text-2xl" style="color:{{c.text_color}}">{{c.sub_title}}</p>
			<a href="{{c.btn_url}}" style="color:{{c.btn_color}}; background:{{c.btn_bg}};">{{c.btn_txt}}</a>
		</div>
	</div>
</section>

